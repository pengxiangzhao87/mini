
 
  function numberFormat(value) {
    console.info('common')
          var v = parseInt(value)//强转Int，毕竟有可能返回是String类型的数字
          return v.toFixed(2)
  }
 
module.exports = {
  numberFormat:numberFormat
}
